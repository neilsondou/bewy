fn main() {
    let x: i32 = "hello";  // type mismatch
    println!("{}", undefined_var);  // undefined variable
    let items = vec![1, 2, 3;  // missing closing bracket

    let y: String = 42;  // type mismatch
    if true {
        println!("test")
    // missing closing brace
}
