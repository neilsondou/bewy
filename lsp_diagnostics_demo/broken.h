#ifndef BROKEN_H
#define BROKEN_H

struct MyStruct {
    int x;
    char* name
    float values[;  // missing array size and bracket
};

int add(int a, int b)  // missing semicolon on declaration
void missing_return_type;  // not a valid declaration

#endif
