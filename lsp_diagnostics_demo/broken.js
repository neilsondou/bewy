function greet(name) {
  console.log("Hello, " + name)
}

const result = greet(;  // unexpected token
let x = [1, 2, 3;  // missing closing bracket
console.log(undefinedVar + 1);  // undefined variable
