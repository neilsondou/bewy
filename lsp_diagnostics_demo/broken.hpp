#pragma once
#include <string>

class MyClass {
public:
    MyClass(int val) : value(val {}  // missing closing paren
    std::string getName( { return name; }  // missing closing paren
private:
    int value;
    std::string name
};  // missing semicolon on member above
