fun main() {
    val name: Int = "hello"  // type mismatch
    println(undeclaredVariable)  // unresolved reference
    val items = listOf(1, 2, 3
    // missing closing paren

    fun add(a: Int, b: Int): Int {
        return a + b
    // missing closing brace
}
