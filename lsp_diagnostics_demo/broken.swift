import Foundation

func greet(name: String) -> Int {
    return "Hello, " + name  // type mismatch: returning String as Int
}

let number: Int = "abc"  // type mismatch
var items = [1, 2, 3
// missing closing bracket
print(undeclaredVariable)  // undefined variable
